import 'package:flutter/material.dart';

class Screen_category extends StatelessWidget {
  const Screen_category({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("category");
  }
}
