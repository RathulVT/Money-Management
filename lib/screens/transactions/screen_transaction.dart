import 'package:flutter/material.dart';

class Screen_trans extends StatelessWidget {
  const Screen_trans({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("data");
  }
}
